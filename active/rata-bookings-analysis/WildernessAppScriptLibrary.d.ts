declare namespace WildernessAppScriptLibrary {
  class Utility {
    isInWorkingHours(startHour: number, endHour: number): boolean;
  }

  function getSharedDataclipUrl(dataclipId: string, sheetName: string): string;
}
